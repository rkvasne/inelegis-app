# ============================================
# INELEGIS - Variáveis de Ambiente
# ============================================
# 
# IMPORTANTE: 
# - Este arquivo é apenas um TEMPLATE
# - NÃO adicione valores reais aqui
# - Para desenvolvimento local, copie para .env.local
# - Para produção, configure no Vercel Dashboard
#
# ============================================

# --------------------------------------------
# Redis - Analytics Database
# --------------------------------------------
# Obtenha em: Vercel Dashboard → Storage → seu database
# Formato: redis://user:password@host:port
#
REDIS_URL=

# --------------------------------------------
# Analytics Dashboard - Token de Acesso
# --------------------------------------------
# Gere um token seguro para acessar o dashboard
# Exemplo: openssl rand -hex 32
#
ANALYTICS_ADMIN_TOKEN=

# --------------------------------------------
# Environment
# --------------------------------------------
NODE_ENV=production

# --------------------------------------------
# Redis Maintenance / Cron
# --------------------------------------------
# Token opcional para proteger o endpoint /api/redis-maintenance
CRON_SECRET=
# Dias mantidos na lista de histórico (padrão: 30)
REDIS_RETENTION_DAYS=30
# Máximo de entradas por usuário (padrão: 100)
REDIS_MAX_HISTORY=100
# TTL em segundos aplicado às listas history:* (padrão: 31.536.000s ~ 365 dias)
REDIS_HISTORY_TTL=31536000
# Dias que os snapshots semanais permanecem no Redis
REDIS_METRICS_TTL_DAYS=120
# Hash onde as métricas são armazenadas
REDIS_WEEKLY_METRICS_KEY=history:metrics:weekly

# ============================================
# SETUP RÁPIDO
# ============================================
#
# Desenvolvimento Local:
# 1. cp .env.example .env.local
# 2. Preencha os valores do Vercel Dashboard
# 3. npm run dev
#
# Produção (Vercel):
# 1. Configure no Vercel Dashboard
# 2. Settings → Environment Variables
# 3. git push (deploy automático)
#
# Documentação: docs/guides/SETUP-REDIS.md
# ============================================
